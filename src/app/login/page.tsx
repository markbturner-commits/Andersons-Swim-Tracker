// Placeholder — Lane A fills this in with magic-link Supabase auth + invite-only signup-gate copy.
export default function LoginPage() {
  return (
    <main className="grid min-h-screen place-items-center bg-white p-6">
      <div className="w-full max-w-sm text-center">
        <h1 className="font-display text-3xl text-navy">Anderson&apos;s Swim Tracker</h1>
        <p className="mt-2 text-ink">Track every meet. See if you&apos;re getting faster. That&apos;s it.</p>
        <p className="mt-8 text-sm text-ink/60">Sign-in flow ships with Lane A.</p>
      </div>
    </main>
  );
}
